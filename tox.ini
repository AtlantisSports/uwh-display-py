[tox]
envlist = py{34}

[testenv]
deps =
    pytest
    pytest-cov
    pytest-watch
    pyflakes

commands =
    {posargs:py.test}
    pyflakes uwhd

passenv =
    DISPLAY

[pytest]
addopts =
    --cov=uwhd
    --cov-report=term-missing
    --cov-report=xml

norecursedirs =
    .git
    .tox
